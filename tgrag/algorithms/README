## Dataset Requirements

### Input Files

The algorithm expects two CSV files in the CrediGraph temporal format:

1. **Nodes file** (`temporal_nodes.csv`): Contains node information
   - Required column: `node_id` (unique identifier for each node)
   - Optional: Additional node attributes (domain, time_id, etc.)

2. **Edges file** (`temporal_edges.csv`): Contains graph connectivity
   - Required columns: `src` (source node), `dst` (destination node)
   - Optional: Additional edge attributes (time_id, weights, etc.)

### Toy Datasets

For testing and development, you can use sample datasets. Default paths expect data in:
- `../../data/dqr/temporal_edges.csv`
- `../../data/dqr/temporal_nodes.csv`

### Configuring File Paths

To use different datasets, modify the path constants at the top of the file:

```python
# Current default paths
TEMPORAL_EDGES_PATH = "../../data/dqr/temporal_edges.csv"
TEMPORAL_NODES_PATH = "../../data/dqr/temporal_nodes.csv"

# Output paths
NEW_TEMPORAL_EDGES_PATH = "../../data/dqr/new_temporal_edges.csv"  
NEW_TEMPORAL_NODES_PATH = "../../data/dqr/new_temporal_nodes.csv"
```

## Requirements

The implementation depends on the following packages:
- `pandas>=2.2.3`
- `matplotlib>=3.9.4`

## Validation Tests

The PageRank implementation includes four validation tests:

1. **Convergence Test**: Verifies the algorithm converged within the maximum iterations
2. **Score Sum Test**: Ensures all PageRank scores sum to 1.0 (probability conservation)
3. **Positive Values Test**: Confirms all scores are positive (> 0)
4. **Degree Correlation Test**: Validates relationship between node connectivity and PageRank scores

All tests must pass for reliable results.